---
apiVersion: v1
kind: Service
metadata:
  name: github-stats-metrics
  labels:
    app: github-stats
    app.kubernetes.io/component: exporter
spec:
  selector:
    app: github-stats
  ports:
    - name: http
      port: 8080
      targetPort: http
